import { ShowListDirective } from './show-list.directive';

describe('ShowListDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowListDirective();
    expect(directive).toBeTruthy();
  });
});
